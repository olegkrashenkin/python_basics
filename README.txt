7 задание не сделал - матан не моя тема.
В складе логика такая:
Запускается программа - пользователь видит главное меню
с вариантами операций.
4 - Выход - тут все понятно)
2 - Принять товар - пользователь выбирает какой вид товара надо добавить на склад,
вводит производителя и модель, в этот момент из этих данных формируется
артикул товара и начинается проверка по нему в файле vendor_code.txt,
если программа находит совпадения, то подсасывает оттуда карточку товара и
(значит такой товар уже есть в базе) перезаписывает строку в файле database.txt,
которая отвечает за этот товар - меняет количество и дату обновления информации.
Если программа не находит совпадения по артикулу в файле vendor_code.txt,
то отправляет в класс выбранного устройства для заполнения карточки товара,
после делает запись в database.txt(количество и прочая инфа) и vendor_code.txt(карточка товара).
1 - Посмотреть наличие товара - показывает информацию о всех товарах в выбранной категории.
3 - Отгрузить товар - то же что и приемка, только наоборот=) Если товара нет - выводит сообщение,
если отгрузить хотят больше чем есть - выводит предупреждение, если все ок,
то изменяет количество и дату обновления инфы в строке товара в database.txt

Обработку исключений сделал далеко не везде - недостаток времени,
Так же хотел добавить логирование, чтобы можно было отследить судьбу товара=)
когда первый раз поступил на склад, сколько раз отгружался, сколько раз пополнялись запасы,
но в принципе по коду это надо просто взять что есть и переставить пару строк местами -
не очень интересно и опять же время=)

В общем вышло давольно по-босяцки, но задание само по себе огонь)

Очень жду ваших замечаний, уверен что косяков у меня тут куча)